[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyowl-core"
version = "0.1.1"
description = "Java-free shared OWL 2 structural parsing and communication kernel"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE", "THIRD_PARTY_LICENSES/*"]
authors = [{ name = "pyowl-core contributors" }]
keywords = ["OWL", "OWL2", "ontology", "semantic-web", "reasoning"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
  "Topic :: Scientific/Engineering :: Information Analysis",
  "Typing :: Typed",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/OAEI-ML/pyOWLCore"
Repository = "https://github.com/OAEI-ML/pyOWLCore"
Documentation = "https://github.com/OAEI-ML/pyOWLCore/tree/main/docs"
Issues = "https://github.com/OAEI-ML/pyOWLCore/issues"

[project.optional-dependencies]
dev = [
  "build>=1.2",
  "hypothesis>=6.100",
  "mypy>=1.10",
  "pytest>=8",
  "pytest-benchmark>=4",
  "ruff>=0.5",
  "tomli>=2; python_version < '3.11'",
  "twine>=5",
]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
pyowl_core = ["py.typed"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.10"
strict = true
