# WP15 — retained native arena and lazy Python facade

Status: implementation checkpoint; million-unique native/Python facade bound
and exact-commit local native safety/lifecycle slices proved, remaining hosted
and breadth gates open
Prepared: 2026-07-21
Capability claim: none

## Outcome

WP15 has a checked retained Rust arena, an opaque owning publication handle,
and an implementation of the lazy Python document/snapshot surface. The
original `NativeSnapshotPublicationV1` remains the immutable arena/freeze
foundation. Contract testing found that V1 could not satisfy WP15's lazy-facade
acceptance criteria because it exposed counts and ownership but no bounded,
typed scalar query protocol.

`NativeSnapshotPublicationV2` is therefore an explicit versioned amendment,
not a positional extension of V1. It adds a generated semantic ledger,
attestation, bounded page/contains requests, cardinality summaries, auxiliary
row codecs, exact owner roles, and native/Python counters. Its private facade
materializes model and report values on demand, retains the exact owner, and
uses a bounded process-local LRU instead of an ontology-sized permanent Python
cache.

Commit `43e4785` restores continuous native safety lanes and records a clean,
exact-revision local checkpoint. AddressSanitizer and ThreadSanitizer each pass
all 211 production Rust library tests, the dependency-free Miri ownership
harness passes all 13 tests, and the Functional and wire targets each complete
10,000 AddressSanitizer-backed libFuzzer runs without a finding. This closes a
concrete missing harness/buildability gap; it does not stand in for the full
interpreter, platform, format, or scheduled campaign matrices.

Commits `b91bb91` and `0bc75d4` add an actual interpreter-policy lifecycle
matrix and a reproducible subinterpreter probe. The dispatcher now recognizes
Python 3.14's public `concurrent.interpreters` identities before the legacy
private API, so an unsupported subinterpreter cannot be mistaken for the main
interpreter and proceed to extension import. A clean CPython 3.12 test-hook run
passes owner close/GC, teardown, real fork reset, signal, cancellation, GIL,
panic, and thread cases; CPython 3.11 creates, exercises, and destroys eight
subinterpreters through the complete Python fallback without attempting a
native import.

Commit `e7abee8` adds deterministic allocation-failure injection at the actual
PyO3 retained-component boundary. The suite discovers the allocation count for
each of the 76 generated model constructors, injects a failure at every build,
freeze, and encode checkpoint, then proves the first non-failing boundary still
round-trips exact canonical bytes. The exact local sweep covers 20,673
checkpoints: 4,460 during construction, 16,137 during canonical freeze, and 76
during encoding. Build failures poison later mutation and freeze, freeze
failures publish no owner, and encode failures leave the immutable arena
recoverable.

Commit `1a17943` extends that fail-closed evidence through the validated V2
publication builder. A private test hook counts every positive retained or
temporary allocation claim, injects failure before budget mutation at each of
the 57 discovered checkpoints, and returns no owner on failure. The first
non-failing boundary publishes the exact attested owner, and every attempt
leaves the caller's fixture collections unchanged. This is publication-budget
coverage, not a claim that a process allocator campaign has run.

Commit `269df83` then covers the direct encoded-view Python bridge. Its private
probe injects before all 51 explicit backing-bytes, owner-memoryview, slice,
dictionary, and counter-publication checkpoints. Every failed attempt returns
no buffers, leaves the retained owner open, and records no successful view
request; checkpoint 51 publishes the exact eleven independently decodable
buffers through one Python bytes owner. Rust encoded-column workspace and
process-allocator failures remained separate open slices at that checkpoint.

Commit `6073ed5` closes the retained encoded-column Rust workspace slice. The
test hook follows the production table-capture and direct-fill preparation
path and injects before each actual capacity growth or owning-box allocation.
The fixture exposes 13 checkpoints spanning retained root-table capture, graph
discovery, canonical-length state, canonical sorting/cursors, and node-ID
publication. Every failure returns no buffers, leaves the owner open, and does
not increment the successful-view counter; checkpoint 13 reproduces the exact
eleven-buffer view. Parser, remaining bridge, and process-allocator failures
remain separate open slices.

Commit `129c96b` adds a Functional parser session allocation-budget sweep. A
private hook injects before each of the 38 positive token/model/result memory
claims exercised by the representative source, before the live-memory budget
is mutated. Every failure publishes no parse result and preserves the mutable
caller source; checkpoint 38 reproduces the exact framed result. This closes
the parser session-accounting slice, not the source/Python bridge or a process-
allocator campaign.

Commit `3c580e5` then covers the Functional source/result Python bridge. Its
private probe injects before seven explicit builtins lookup, memoryview,
source-copy, owned-Vec, and framed-result publication checkpoints. Every
failure returns no result and leaves the mutable caller source unchanged;
checkpoint 7 reproduces the exact framed parse result. Configuration and other
bridge paths plus the process allocator remain separate open slices.

Commit `87e66f6` extends that probe through the Functional configuration bridge.
The combined campaign now injects before 13 explicit configuration/source
builtins lookups, memoryviews, copies, owned-Vec allocations, and framed-result
publication checkpoints. Every failure returns no result and leaves both the
mutable configuration and mutable source unchanged; checkpoint 13 reproduces
the exact framed parse result. Other bridge paths and the process allocator
remain separate open slices.

Commit `445ecf2` covers the native index request/source/result Python bridge and
replaces its request's infallible `to_vec` copy with an explicitly reserved,
fallible owned buffer. The private probe injects before all 13 builtins,
memoryview, copy, owned-Vec, and framed-result publication checkpoints. Every
failure returns no result and leaves the mutable request and source unchanged;
checkpoint 13 reproduces the exact framed index result. Remaining bridge paths
and the process allocator stay open.

Commit `c1f7d4e` covers the canonical validation, wire validation, and wire
roundtrip Python bridges. Each operation exposes 13 explicit configuration/
input builtins, memoryview, copy, owned-Vec, and result-publication checkpoints,
for 39 injected failures and three exact first-non-failing boundaries. Every
failure publishes no result and preserves the mutable configuration and input;
the boundaries reproduce the exact canonical bytes, validation receipt, and
canonical wire roundtrip. Remaining bridge paths and the process allocator
stay open.

Commit `beb31b1` adds the missing verified-mapping wire path. Encoding a mapped
snapshot now revalidates the borrowed image under the caller's resource limits,
holds the mapping lease through the copy, and returns the exact wire bytes
without materializing the structural model. The complete generated-constructor
fixture proves native/Python/wire identity, mapped-column ownership, ordering,
fingerprints, hashes, resource failures, and close fencing. An unverified
mapping with a corrupted digest withholds `wire-verified` and rebuilds trusted
bytes instead. This is one exact local mapping checkpoint, not the retained-V2,
installed-artifact, full-format, or supported-platform matrix.

Commit `8799246` advances the private extension ABI to version 2 after the
retained Functional entry point gained its final import-policy argument. The
Python probe now rejects an ABI-1 artifact at metadata validation before
calling native self-test or operation code, so a stale developer artifact
cannot be selected and fail later with an untyped PyO3 arity error. A clean
detached ABI-2 artifact passed 71 focused Python tests plus 157 constructor
subtests, all 220 production Rust tests, rustfmt, strict Mypy, Ruff, and
all-feature Clippy with warnings denied.

Commit `ce8cbba` also restores the semantic-reference boundary around
`PythonParser`. Backend orchestration now lives above the Python parser, so a
direct semantic-reference parse cannot probe, import, or dispatch into the
native extension even when its input options request the native backend. At
detached revision `c8f18e7`, the complete suite passed 1,139 tests plus 3,546
subtests with seven explicit environment/platform skips against the exact ABI-2
artifact. This is clean-tree regression evidence for the parser and extension
boundaries, not completion of the WP15 platform or lifecycle matrix.

Commit `360e82d` closes the previously missing million-unique Python-boundary
proof. A test-hook-only Rust generator constructs one million canonically
ordered, distinct `Declaration(Class(IRI(...)))` rows without a proportional
Python row or model collection, retains them behind the exact registered
`_NativeSnapshotHandle`, and binds only bounded V2 publication metadata in
Python. The public lazy snapshot then traverses all one million scalar axioms
in order while discarding each result. Before access it has zero model
materializations and 21 publication objects; after the complete traversal its
LRU retains 256 entries/183,960 bytes and Python GC sees only 1,637 additional
tracked objects, not an ontology-sized duplicate.

This checkpoint does not advertise `retained-native-load-v1`, a syntax format,
or the encoded structural view. The million-unique generator and the
Python-backed publication constructor remain test hooks. Separately, the WP16
Functional checkpoint now uses the typed Rust construction/freeze seam and
retains the real component arena without a proportional Python collection;
the million fixture deliberately isolates the V2 owner/facade object and cache
contract rather than claiming parser or format coverage.

## Frozen amendment

- `schemas/native-snapshot-publication-v2.toml` is generated from the exact
  typed semantic tree. Its canonical digest binds the envelope, attestation,
  page and contains records, cardinality records, owner members, row codecs,
  ordering, duplicate policy, counters, resource bounds, and capability rules.
- V2 requires named construction and exact public/private record types. Any
  semantic, lifetime, ordering, or field change requires V3.
- Structural rows are canonical-model-v1 bytes. Effective structural and
  origin collections are canonical ascending and unique. Raw source-map and
  raw document-origin collections preserve their separately specified producer
  multiplicity/order while supporting digest-group paging.
- Page requests are bounded by rows, bytes, actual maximum retained row size,
  publication `ParseLimits`, scope, document ordinal, signature selection, and
  the optional digest filter. Totals and page boundaries are pinned for a
  traversal and fail closed on drift.
- Snapshot requests observe effective document/closure collections. Document
  owners observe raw document structural collections where the public document
  contract requires them. Source maps remain raw for both owner roles.
- A handle owns its retained storage. Snapshot forks and document handles have
  independent close state while sharing immutable storage. No borrowed pointer
  or buffer is part of the Python handoff.
- Optional source-map, origin, RDF mapping, and OWL 2 DL collections are
  capability/option gated before an owner call. Diagnostic and validation
  sequences preserve producer order and multiplicity.
- Native and Python counters separate retained component/canonical/auxiliary/
  metadata bytes, builder/freeze peaks, page copies, decoded/materialized rows,
  cache state, parser/wire/view calls, and base flattening.

## Deliverable ledger

| WP15 deliverable | Status | Evidence / remaining work |
|---|---|---|
| Checked mutable builder and immutable arena | implemented foundation | Native model/component builders intern all generated model tags, freeze deterministic dense tables, and retain immutable owners. Full parser-produced closure integration remains a WP16 cross-gate. |
| Opaque snapshot/document ownership | implemented checkpoint | V1/V2 handles are private, owner-registered, send/sync checked, independently closeable, fork-aware, and storage-sharing. Exact-revision local ASan, TSan, and pure-owner Miri lanes pass; the complete interpreter/platform lifecycle matrix remains open. |
| Frozen builder-to-snapshot handoff | amended to V2 and consumed by a guarded production slice | V1 remains immutable; V2 supplies the bounded facade protocol omitted by V1. The generated ledger and Python/Rust fixture parity exist, and WP16's guarded Functional path now publishes the production typed Rust owner through the handoff. Complete format/closure use remains WP16 work. |
| Lazy public facades | implemented checkpoint | Documents, snapshots, canonical sets, source/origin mappings, reports, signatures, scope traversal, equality/hash, copy/pickle, context-manager, and close behavior have focused tests. |
| Bounded materialization/cache | million-unique acceptance proof passes | The exact Rust V2 owner publishes one million unique axioms with 21 Python publication objects and no scalar pages/materializations before access. Full ordered scalar traversal emits exactly one million rows through 15,625 bounded pages; the final cache is 256 entries/183,960 bytes with 999,744 evictions, and the post-GC tracked-object delta is 1,637. Approved-machine RSS evidence remains a performance-release gate rather than an object/cache correctness gap. |
| Generated/model fixture path | implemented | V1 arena fixtures and V2 generated Python/PyO3 owner fixtures exercise construction, paging, containment, reports, malicious owners, digests, limits, and lifecycle. |
| Mapped/wire retained fixture path | local mapped-owner checkpoint passes; retained-V2 cross-product open | The complete generated-constructor mapping preserves its owner through exact wire copying and encoded-column publication without eager model reconstruction. The final retained V2 mapped-owner, installed-artifact, format/option/import, and supported-platform cross-product remains owned with WP17. See `reports/security/native-mapped-wire-checkpoint.json`. |
| Allocation/object/lifetime instrumentation | partial, component/publication/wire/direct-view-bridge/encoded-workspace/parser-session/parser-bridge/index-bridge/foundation-bridge failpoint and local native-safety checkpoints complete | Disjoint counters, transactional counter updates, retained/temporary budgeting, sparse raw overrides, close/fork/concurrent-read tests, cache gauges, before/after GC counts, and the million-unique owner/facade proof exist. All 20,673 explicit component build/freeze/encode checkpoints, 57 positive V2 publication-budget claims, five native wire validation/receipt allocations, 51 direct-view Python bridge checkpoints, 13 actual Rust encoded-column workspace growth checkpoints, 38 positive Functional parser session claims, 13 Functional configuration/source/result bridge checkpoints, 13 native index request/source/result bridge checkpoints, and 39 canonical/wire foundation bridge checkpoints fail closed locally. Other bridge/process-allocator paths plus complete interpreter, supported-platform, and hosted evidence remain open. |
| Empty successor seams | implemented | Ingestion and view registration partitions remain independently owned and advertise no successor capability at this checkpoint. |

The extension ABI is now 2. ABI 1 is intentionally incompatible with the
frozen retained-parser call surface and is rejected before native execution;
the public model schema and wire format remain unchanged at 1 and 1.1.

## Million-unique verification checkpoint

At commit `360e82d`, a release test-hook extension was rebuilt at
`/private/tmp/pyowlcore-wp15-test-hooks-target/release/lib_native.dylib`. The
explicit acceptance command was:

```text
PYTHONPATH=src \
PYOWL_CORE_TEST_NATIVE_LIBRARY=/private/tmp/pyowlcore-wp15-test-hooks-target/release/lib_native.dylib \
PYOWL_CORE_RUN_WP15_MILLION=1 \
.venv/bin/pytest -q -s \
  tests/native/retained_facade/test_million_unique_owner.py::test_million_unique_native_owner_stays_python_object_and_cache_bounded

1 passed in 305.51s
```

The exact emitted evidence was:

```text
unique_axioms=1000000
row_bytes=53
canonical_input_bytes=53000000
publication_objects=21
gc_objects_before_publication=50278
gc_objects_after_publication=50366
gc_objects_after_traversal=52003
model_rows_materialized=1000000
page_requests=15625
rows_emitted=1000000
cache_current_entries=256
cache_current_bytes=183960
cache_peak_bytes=183960
cache_evictions=999744
retained_owner_bytes=109001713
peak_builder_live_bytes=167721969
```

Thus publication added 88 GC-tracked Python objects and full traversal left a
1,637-object delta over the post-publication baseline. Native counters prove
one million unique canonical input rows, two million document/closure row
references sharing retained payloads, no structural publication rows copied
back into Python, and exact page/row/payload totals. The test verifies every
decoded axiom's exact type, fixed-width IRI ordinal, and canonical ordering;
it never builds a Python tuple of the million results.

The 257-row always-on smoke case also passed against the rebuilt extension.
Ruff and strict Mypy passed for the new proof, production and test-hook Clippy
passed with warnings denied, and the isolated Rust test configuration passed
all 188 tests. This checkpoint is object/cache correctness evidence, not an
approved-machine RSS benchmark or a substitute for the remaining lifetime and
cross-constructor matrix.

## Native safety verification checkpoint

The machine-readable record is
`reports/security/native-safety-checkpoint.json`. Against clean detached commit
`43e478591dc7bc3e3d66c22c5a36fec595975422` on macOS x86-64, pinned nightly
`nightly-2026-07-14` produced:

```text
AddressSanitizer native library: 211 passed, 0 findings
ThreadSanitizer native library plus sanitized std: 211 passed, 0 findings
Miri pure ownership harness: 13 passed, 0 findings
Functional parser ASan libFuzzer: 10,000 runs, 0 crashes/findings
wire decoder ASan libFuzzer: 10,000 runs, 0 crashes/findings
```

The fuzz campaigns used disposable corpus copies. The SHA-pinned
`native-safety.yml` workflow is configured but has not run as part of this
local checkpoint, so the record says `configured-not-run`. Miri covers the
pure production ownership modules and deliberately excludes the CPython FFI.
The aggregate security/resource/determinism release row remains `not-run`.

## Retained allocation-failure checkpoint

The machine-readable record is
`reports/security/native-allocation-checkpoint.json`. A clean detached checkout
at `c1f7d4e95e1897fa4e816b2ea4b2d733271e177d` built a checksummed test-hook
extension and passed 303 Python tests plus 3,144 subtests across retained,
publication, wire, and encoded-view boundaries. The dynamic all-constructor sweep injected and observed
20,673 typed component-allocation failures, then passed 228 exact component
phase-boundary successes. It also injected each of the five explicit native
wire validation/receipt allocations, published no failed receipt, preserved a
mutable caller input, and reproduced the exact receipt at the first
non-failing boundary. The validated V2 publication builder likewise injected
all 57 positive allocation-budget claims before mutation, returned no partial
owner, preserved its fixture mappings, and reproduced the exact attested owner
at checkpoint 57. The direct encoded-view bridge injected all 51 explicit
Python object/publication checkpoints, returned no failed buffers, preserved
owner usability and the successful-request counter, then reproduced the exact
eleven-buffer view at checkpoint 51. The retained encoded-column workspace
probe injected all 13 actual growth/owner allocations, published no failed
view, preserved owner usability and the counter, and reproduced the same exact
view at checkpoint 13. The Functional parser session probe injected all 38
positive allocation-budget claims before mutation, published no failed result,
preserved its caller input, and reproduced the exact result at checkpoint 38.
The Functional configuration/source/result bridge probe injected all 13
explicit Python object/copy/publication checkpoints, returned no failed result,
preserved both mutable caller inputs, and reproduced the exact framed result at
checkpoint 13.

The native index request/source/result bridge probe injected all 13 explicit
Python object/copy/publication checkpoints, returned no failed result,
preserved both mutable caller inputs, and reproduced the exact framed result at
checkpoint 13.

The foundation bridge probe injected 13 checkpoints for each of canonical
validation, wire validation, and wire roundtrip. All 39 failures published no
result and preserved mutable configuration/input buffers; the three boundaries
reproduced exact canonical bytes, the validation receipt, and canonical wire.

The same revision passed 231 Rust library tests,
test-hook Clippy with warnings denied, rustfmt, focused Ruff, and strict stub
Mypy.

This closes the retained component builder/freeze/encoder and native wire
validation/receipt slices plus the retained publication-budget and direct-view
bridge, Rust workspace, Functional parser-session, and configuration/source/
result, native index, and canonical/wire foundation bridge slices, not the
whole release gate. Process-allocator failure, other bridge paths, installed-
wheel, hosted-
interpreter, and supported-platform evidence remain open, so the aggregate
security/resource/determinism row stays `not-run`.

## Interpreter lifecycle checkpoint

The exact record is `reports/security/native-lifecycle-checkpoint.json`. At
clean detached revision `0bc75d42003e43fc3adddbf3745355e406e13843`, a locally
built owning test-hook extension produced 17 passing tests and 157 passing
subtests across metadata, retained-owner close/GC, interpreter teardown, real
fork reset, signal, cancellation, panic, GIL-release, and concurrent work.

The reusable command

```text
PYTHONPATH=src python3.11 -m tools.security.subinterpreter_probe --repetitions 8
```

also passed: eight interpreters were created and destroyed, eight Functional
documents used `AUTO`'s complete Python fallback, and the guarded native import
count remained zero. Local CPython 3.12.3 is explicitly `not-run` for that same
subinterpreter slice because an isolated preflight reproduces an interpreter
abort using the standard-library `ssl` module alone. Local CPython 3.14 and
3.14t are unavailable. The checked workflow covers 3.10, 3.12, 3.14, and 3.14t
but remains `configured-not-run`, so this is not a complete lifecycle matrix.

## Acceptance state

WP15 acceptance remains open. The typed production handoff and million-unique
object/cache requirements now have direct evidence. The following are still
release-blocking for this work package rather than deferred implementation
details:

- complete remaining bridge and process-allocator failure
  coverage beyond the passing retained publication-budget and direct-view
  bridge sweeps,
  plus the remaining cancellation, panic, close/fork/concurrency,
  interpreter-lifecycle, supported-platform, and hosted continuous matrices
  beyond the passing local native-safety and CPython 3.11/3.12 slices;
- exercise generated/native-arena and mapped/wire owners across every model
  constructor with exact canonical, fingerprint, order, diagnostic, and hash
  parity; and
- freeze the final verification commands/results and exact handoff revisions in
  this report.

Until those gates pass, WP15 makes no retained-load capability claim. WP16 and
WP17 production checkpoints must remain fail-closed and unadvertised until
their own acceptance matrices pass. Neither successor may reach into the
other's module or treat either test-hook publication constructor as a
production native path.
