# WP23 — component-scoped anonymous canonicalization v2

Status: implementation and scoped DOID comparison complete; immutable
release-candidate, remaining public-corpus, platform, and publication gates pending
Prepared: 2026-08-02
Release claim: not made

## Outcome

Anonymous graphs are now partitioned into connected components before
canonical refinement. Document-wide labels and arcs remain charged to
`max_terms`, while `max_canonical_work` is enforced independently for each
component. Complete canonical component graph bytes, a sorted
multiplicity-preserving manifest, component-class occurrence ordinals, and
model-schema-2 identity domains prevent disconnected isomorphic anonymous
individuals from collapsing.

Python and retained-native implementations use the same scheme. Independent
reference vectors exercise source-label, root-order, component-order, and hash
seed permutations, including repeated isomorphic components and true duplicate
roots. One oversized connected component still fails closed through WP19's
typed `ResourceLimitError` contract.

The coordinated public version ledger is implemented as package `0.2.0`, API
`(0, 2)`, model schema `2`, wire `(1, 2)`, adapter protocol `1`, and encoded
structural schema `2`. WP22's frozen
`LoadOptions.allow_partial_rdf_mapping=False` API and native V2 option ledger
are incorporated. Model and encoded schema 1 remain frozen and are not
reinterpreted.

This is an implementation and local-artifact checkpoint, not full incident
closure or a portable release claim. A clean native wheel built from the exact
`21f4e418da005e260f6056dc9ce0ac4a8af4210d` implementation revision passed its
installed smoke and the requested DOID comparison. That revision predates this
evidence/report commit, the required one-pass public-corpus and hosted-platform
matrices have not all run, and the release gates remain blocked.

## Deliverable ledger

| Deliverable | Checkpoint state | Repository evidence |
|---|---|---|
| bounded component partitioning | implemented | `src/pyowl_core/document/document.py`, `native/src/parse/anonymous.rs` |
| global term accounting and per-component canonical-work limit | implemented and focused-tested | Python/native anonymous and component-canonicalization tests |
| complete component graph, multiplicity manifest, occurrence slots, v2 domains, cached first-phase order | implemented | Python/native anonymous implementations and independent vectors |
| invariance without anonymous-individual collapse | implemented and focused-tested | `tests/reference/anonymous_v2.py`, `tests/unit/model/test_anonymous_components_v2.py` |
| Python/native canonical algorithm equivalence | implemented and exact-checkpoint tested locally | Rust/Python vectors and forced-native tests pass; hosted-platform and immutable final-revision reruns remain open |
| model schema 2, encoded schema 2, and wire minor 2 | generated, registered, and schema-checked | `schemas/model-v2.toml`, `schemas/encoded-view-v2.toml`, `schemas/encoded-view-v2.json`, `schemas/version-decision-v2.toml` |
| package/API/model/wire/adapter/encoded release ledger | implemented in source, metadata, docs, fixtures, and workflows | package `0.2.0`, API `(0, 2)`, model `2`, wire `(1, 2)`, adapter `1`, encoded schema `2` |
| component phase/work/structural telemetry | implemented | sixteen `native_anonymous_*` values in public `LoadReport.timings` |
| component-phase allocation telemetry | implemented and focused-tested | `native_anonymous_accounted_bytes` is the exact monotonic `Session` memory-budget delta across common anonymous scoping |
| public Functional component-scaling gate | release profile **passed**; functional evidence only | `reports/performance/component-canonicalization-v2/release.json`, evidence SHA-256 `d950d8d2...` |
| NCIt one-document gate | candidate observation **passed**, closure incomplete | one native/default-limit document loaded; final-revision retention, model-schema-1 alpha/count reference, and same-machine RSS comparison remain open |
| FMA and licensed SNOMED incident closure | **not run** | FMA public gate remains open; licensed SNOMED absence blocks only the private incident claim |
| installed native vs py-horned on DOID | scoped two-lane comparison **passed** for equality, common-ready wall, and fresh RSS | `reports/performance/native-redesign/doid-0.2.0-installed-vs-py-horned.json`; non-formal because the Python reference lane and approved reference host were outside the requested scope |

## Canonicalization smoke evidence

The deterministic smoke profile was generated and independently checked with:

```text
PYTHONPATH=src .venv/bin/python -m tools.benchmark.component_canonicalization generate --profile smoke --output <temporary-path>/smoke.json
PYTHONPATH=src .venv/bin/python -m tools.benchmark.component_canonicalization check <temporary-path>/smoke.json
```

Both commands produced the same evidence SHA-256:

```text
fb32ab633f157c50d97d4b511fb965caba4454daf3aeebd11d8d0c842319b2e9
```

An independent file hash check agreed. The pinned input-lock SHA-256 was
`b03be31e945465800b5713caa9e5f544c10b0a72c2967588736071b97c5fbbd0`.

The three fixed-size cases contain disconnected one-label components. Each
component charges exactly 9 work units: setup 3, refinement 4, and candidate
order 2.

| Case | Components | Total work | Largest component work | Canonical fingerprint |
|---|---:|---:|---:|---|
| `fixed-1` | 1 | 9 | 9 | `1e4e978b32f92d8a1cd63584836339095104f33dedb69da9b021b080736e459d` |
| `fixed-8` | 8 | 72 | 9 | `981f21b791b03747e7216cba773368cde118e2b061ddfc17971f5e23ea74bd0e` |
| `fixed-64` | 64 | 576 | 9 | `96f2634f62ca2d71fe3230eba492b61c78b9601fb1c069c3ca34213b5306d0c3` |

For each fixed case, total labels, arcs, and permutations equal the component
count; the largest component has one label, one arc, and one root; maximum
root span and maximum open intervals are one; and maximum refinement rounds is
one. The evidence checker verifies the input hashes and the additive-work
claim.

`oversized-4` is one connected component with 4 labels and 10 arcs. It fails
with `ResourceLimitError`, code `RESOURCE_LIMIT`, limit
`max_canonical_work`, observed `24`, and allowed `23`. Its stable details are
`component_count=1`, `largest_component_labels=4`,
`largest_component_arcs=10`, `refinement_rounds=0`, and
`work_term="setup"`.

The smoke artifact deliberately records `performance_claim: null`. The checker
rejects unknown, missing, stale, or tampered fields, booleans substituted for
integers, invalid structured errors, and any non-null performance claim. This
is functional scaling evidence only; it is not a portable timing or memory
claim.

The release profile's pinned `fixed-50000` input is 5,000,159 bytes with
SHA-256
`e93b82497f07b7d44abec136010a0999e3434d97e2d5a46aa3fba29a0796868f`.
It passed with 50,000 components, 450,000 total canonical-work units, largest
component work 9, and document fingerprint
`d95eacbbad0f0fc91c567599ece59404ff8e372153763999153ddf6044460832`.
The checked evidence SHA-256 is
`d950d8d2fba15b90e57084076967007876aca346d5a29f1deb78cb90af05611b`.
Regeneration at the `21f4e41` implementation checkpoint produced the same
evidence bytes. `performance_claim` remains null: this closes functional
component scaling, not a portable timing or memory claim.

## Verification evidence

The focused Python model, fingerprint, encoded-view, wire, consumer-manifest,
and benchmark-evidence suite passed:

```text
PYTHONPATH=src .venv/bin/python -m pytest \
  tests/unit/model/test_anonymous_components.py \
  tests/unit/model/test_anonymous_components_v2.py \
  tests/unit/fingerprints/test_schema_v2_vectors.py \
  tests/unit/fingerprints/test_independent_domains.py \
  tests/native/encoded_views/test_schema_ledgers.py \
  tests/native/encoded_views/test_python_fallback.py \
  tests/unit/wire/test_codec.py \
  tests/unit/wire/test_corruption.py \
  tests/unit/wire/test_identity_provenance.py \
  tests/integration/consumers/test_compatibility_manifest.py \
  tests/benchmark/component_canonicalization/test_evidence.py -q

68 passed in 8.95s
```

The independent oracle covers exact vectors, multiplicity, 63
label/root/component permutations, and hash seeds `0`, `1`, and `8675309`.
Focused Python tests also cover global `max_terms`, typed oversized-component
failure fields, interleaved root-interval metrics, late component merges,
exact phase telemetry, random permutations, Functional Syntax parsing, and
snapshot rescoping.

The native source suite passed without Python features after adding exact
anonymous-accounting delta coverage:

```text
cargo test --no-default-features

254 passed; 0 failed; finished in 0.40s
```

The focused native anonymous, encoded-column, and wire filters passed 8, 9,
and 2 tests respectively. They cover constructor and schema ledgers, uniform
enforcement fields, component failure context, scope/manifest vectors,
repeated-component keys and work, raw/effective occurrence digests, wide
integer fields, encoded schema 2, and wire minor 2. `cargo fmt --check` and
`cargo check --all-features` also passed. No `cargo test --all-features` result
is claimed for this checkpoint.

A CPython 3.12 test-hook extension was rebuilt from the checkpoint and the
binding validator plus public Functional and RDF/XML anonymous report routes
passed:

```text
PYOWL_CORE_TEST_NATIVE_LIBRARY=native/target/debug/lib_native.dylib \
  .venv/bin/python -m pytest -q \
  tests/native/foundation/test_binding_seams.py \
  tests/native/formats/test_public_retained_load.py::test_anonymous_re_scope_retains_distinct_raw_and_effective_native_owners \
  tests/native/formats/test_rdfxml_retained_v2.py::test_anonymous_individuals_keep_distinct_raw_and_effective_native_owners

25 passed in 3.16s
```

Targeted Ruff and mypy checks passed for the Python telemetry seam. This
debug artifact is focused evidence only and does not replace the final
candidate-artifact gate.

The complete `tests/native` suite against the same rebuilt test-hook extension
passed with `1527 passed, 3144 subtests passed, 3 skipped in 141.91s`. The skips
were the unsupported CPython subinterpreter/free-threading environments and the
opt-in WP15 million-unique acceptance gate. The combined WP19-WP23 focused
source/integration selection passed with `624 passed, 203 subtests passed in
51.16s`; its retained-anonymous slice passed `20 passed, 219 deselected in
2.03s`.

The schema generators and ledgers passed their repository checks:

```text
PYTHONPATH=src .venv/bin/python -m tools.schema.encoded_view check
PYTHONPATH=src .venv/bin/python -m tools.schema.tags check schemas/model-v2.toml --previous schemas/model-v1.toml
PYTHONPATH=src .venv/bin/python -m tools.wire_reference.check_schema
```

The relevant frozen hashes are:

| Artifact | SHA-256 |
|---|---|
| `schemas/model-v2.toml` | `ca31cac864f5978f6e3d2ce3db28963c5ce8438a0d44619d3199b63323665e37` |
| `schemas/encoded-view-v2.toml` | `4ddceb51a81a8e329f9d6fd640c2757750efd9c85675993bfbcabf37b8e47f61` |
| encoded-view v2 JSON/public descriptor | `c51d0eb7ecf6f29ad3495fe7c40a2ea6741cf03a7cf194d51417bb810df90f51` |
| `schemas/version-decision-v2.toml` | `5466e802b1e378c8ce479444e9940d034a492218bb911ff08fbc6d4406eae047` |

Release/docs tooling and its focused contract tests also passed:

```text
PYTHONPATH=src .venv/bin/python -m pytest \
  tests/packaging tests/release \
  tests/foundation/test_repository_contract.py -q

146 passed in 6.54s
```

Ruff passed over changed Python, strict mypy passed over the four changed
packaging tools, and `git diff --check` passed at the checkpoint.

## Installed artifact and DOID comparison

A clean source archive of implementation commit
`21f4e418da005e260f6056dc9ce0ac4a8af4210d` (tree
`d74e59c6ae8ae125fde5cf311505f4180f9f2b1e`) produced an installed CPython
3.12 macOS x86-64 wheel with no bytecode leakage:

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `pyowl_core-0.2.0-cp312-cp312-macosx_14_0_x86_64.whl` | 1,765,045 | `f969d1fff70844660580b73915f65871d407d5d870e1af8c3498d23fd68a661e` |
| `_native.cpython-312-darwin.so` | 3,295,168 | `226373ef1c8f83603ca1467515ea9c6502e982943777ca0b09cf8c1e506a8032` |

The artifact inspector passed every locally evaluable check, Twine metadata
validation passed, installation into a fresh virtual environment passed the
native self-test, and the import probe was quiet and lazy. Target-platform
checks remain deferred to hosted CI. Because this report and its evidence are
later source changes, these files are an exact implementation-checkpoint build,
not the immutable publication payload.

The requested installed-native versus `py-horned-owl==1.4.0` comparison used
the pinned 6,687,536-byte DOID input, one warmup, three paired repetitions per
lane and process mode, resident bytes, and schedule seed 180643. Every one of
the twelve samples produced the same model-schema-2 common-contract digest.

| Equal boundary | Native median | py-horned median | Native / py-horned paired median (95% CI) | Result |
|---|---:|---:|---:|---|
| fresh common-contract ready | 5.869 s | 58.281 s | 0.10227 (0.09699–0.10401) | 9.78-fold py-horned/native; wall gate passed |
| steady common-contract ready | 4.596 s | 57.455 s | 0.08000 (0.07770–0.08235) | 12.50-fold py-horned/native; wall gate passed |
| fresh incremental peak RSS | — | — | 0.85588 (0.77707–0.89661) | RSS gate passed |
| steady incremental peak RSS | — | — | not evaluable | a sample exceeded the 10-ms sampling-gap bound |

Raw engine-model arrival is a different boundary: py-horned loaded in 0.770 s
fresh and 0.697 s steady, while native load measured 2.730 s and 2.708 s. The
native/py-horned raw-load ratios were 3.54 and 3.81. That boundary excludes the
structural mapping, canonicalization, fingerprints, provenance, diagnostics,
validation, and publication needed to obtain the equal common contract, so it
is retained as phase evidence rather than substituted for the end-to-end
result.

The compact checked evidence is
`reports/performance/native-redesign/doid-0.2.0-installed-vs-py-horned.json`
(SHA-256
`e46fe5002817957f8bda21d517d916819da20c5eb2075eea8af3415d0884bdf6`).
An independent audit reproduced its artifact pins, all twelve projected rows,
common contract, medians, ratios, confidence intervals, and decisions from the
86,992,371-byte raw artifact. This was intentionally a user-scoped two-lane
run: the raw harness is formally contract-invalid only because the Python
common-reference lane was omitted, and the shared host is unapproved. No
portable or full reference-performance claim is made.

## NCIt one-document checkpoint

The pinned 747,403,746-byte NCIt RDF/XML input loaded once through the forced
native backend under default limits with no chunking or resource-limit error.
The fresh-process observation recorded 3,506,377 axioms, 212,228 declarations,
10,825,950 RDF triples, 212,234 signature entities, 376.750 s load wall, and
9,570,557,952 bytes peak RSS. All anonymous-component telemetry values were
zero because this mapped document contained no structural anonymous component.

The retained temporary case has SHA-256
`c96be82e4db359f7fd1b54d6c63b699c9849f4d7610870df8f709fe43d4e58b4`
and uses native extension SHA-256 `226373ef...`, but its package-source capture
predates the final Python canonical-row preflight. It also lacks the required
model-schema-1 alpha/count oracle and a jointly attested same-machine incident
baseline. It therefore proves the one-document/default-limit behavior at an
implementation checkpoint, but it does not close NCIt correctness/RSS or bind
the final candidate.

## Public anonymous telemetry

The native report exposes the following exact non-negative integer values
under the `native_anonymous_` prefix in `LoadReport.timings`:

- component count, total labels, and total arcs;
- largest component labels, arcs, and roots;
- maximum root-interval span and maximum open root intervals;
- total setup, refinement, candidate-order, and canonical work;
- largest component work and maximum refinement rounds;
- total permutations examined; and
- anonymous-phase accounted bytes.

Native accumulation uses checked integer sums. Python rejects negative,
non-exact, or greater-than-`2**53` values before the existing float-compatible
public timing conversion. Syntax parse, result encode, arena construction, and
freeze phase timings remain available separately.

`native_anonymous_accounted_bytes` is the exact non-negative increase in the
operation's monotonic `Session` memory-budget charge from entry into the common
schema-2 anonymous scoping helper through completed raw/effective canonical
rows and occurrence-digest tables. The wrapper's ontology-key construction is
charged to the same operation-wide budget before that checkpoint and is
deliberately excluded from this phase-specific value. The counter records
conservative allocation work; it is not live allocation, allocator high-water
usage, or process RSS. Same-machine peak RSS therefore remains part of the
external final-corpus evidence.

## Remaining acceptance and release gates

The following evidence is still required before WP23 can close:

- rebuild the final `0.2.0` native extension from the exact candidate source
  and rerun forced-native Python parity, including WP20-WP22 strict mapping,
  reification, partial-mode conflict, canonical identity, rescoping, wire, and
  encoded-view routes;
- retain or rerun NCIt against that same immutable artifact, supply its
  model-schema-1 alpha/count reference and same-machine RSS attestation, and run
  pinned FMA as one document with default limits and the required correctness,
  structural, timing, and RSS evidence;
- run both licensed SNOMED lanes when authorized data is available. Their
  absence is `not-run` and blocks only the private incident-closure claim;
- extend the passing scoped DOID installed-wheel/py-horned evidence with the
  Python reference and direct-Horned lanes, approved required corpus/reference
  machine, evaluable steady RSS, and delivered-wheel/direct-engine overhead if
  making the full normative reference-performance claim; and
- complete artifact, platform, advisory, consumer, legal, ownership, tag,
  TestPyPI, provenance/signature, and publishing gates against one immutable
  release candidate.

`reports/release/0.2.0/gates.json` currently records all twelve release gates
as `blocked`: advisory scan, consumer matrix, legal review, name control,
platform artifact audit, project URLs, reference performance, release-owner
approval, signatures, source-tag verification, TestPyPI rehearsal, and trusted
publishing. The expected distribution set is one sdist, one universal wheel,
and 25 native wheels.

The pinned DOID strict-mapping incident belongs to WP20 rather than the WP23
performance claim. Its required first-exception evidence remains governed by
`reports/workpackages/WP20.md` and must not be substituted for the NCIt/FMA or
Functional component-scaling gates. The distinct DOID v2 artifact above is
WP18 comparator evidence and does not replace those large-document cases.

Historical `0.1.x` evidence is retained as historical context only. It is not
relabeled or inherited as proof for model schema 2, wire minor 2, encoded
schema 2, the final native artifact, or the `0.2.0` release candidate.
