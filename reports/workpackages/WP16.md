# WP16 — native streaming parsing and structural mapping

Status: implementation checkpoint; guarded Functional loads keep structural
publication bytes native and the private RDF/XML retained slice is expanding,
complete multi-format/closure acceptance open
Prepared: 2026-07-21
Capability claim: none

## Outcome

WP16 now has a fail-closed retained-owner handoff for a deliberately guarded
Functional Syntax shape: one document, native parsing selected explicitly or
by the existing AUTO size/capability policy, source maps and OWL 2 DL
validation disabled, no RDF mapping report, and no anonymous-node re-scope
difference. Imports may be ignored, recorded unresolved when no resolver can
produce a closure member, or use either resolver-backed policy when the native
parser proves the direct-import set is empty before publication. An actual
resolver-backed import selects complete framing before publication. Provenance
may be enabled or disabled; the latter publishes no origin capability or table.
The Functional parser constructs the typed V2 arena directly from its own
canonical nodes in the same detached native call. For eligible loads, Python
receives only compact ontology/import metadata and fixed-size prepared digest
evidence. Canonical root temporaries, structural/logical/signature fingerprint
preimages, and origin-row encoding remain native; no ontology-sized canonical
byte collection or provenance occurrence object is copied into Python. Native
preparation streams one temporary row at a time from the retained arena,
enforces cancellation and temporary/owner memory limits, and finalization
rechecks all six content digests plus cardinality and capability claims.
Duplicate roots remain deduplicated without losing origin occurrences. The
encoded structural view and PYOCORE writer consume retained columns without
scalar model traversal.

This is a checkpoint, not WP16 acceptance. The native call still returns the
complete parser-result framing for explicit fallback cases, including
anonymous-node re-scoping and RECORD_UNRESOLVED loads whose diagnostic metadata
would exceed the retained publication bound. The guarded public-load promotion
proof is still Functional-only; resolver-backed import closure, Turtle, and
OWL/XML remain outside the path. The bridge is intentionally absent from the
ingestion capability ledger, and the encoded view schema is likewise
unadvertised.

The separate private RDF/XML production seam now also accepts the standard
`rdf:parseType="Resource"` property form. It emits the implicit blank node and
nested property edges during the forward XML scan, maps structural class
expressions exactly, and publishes the result through the typed retained V2
owner without crossing the Python RDF/XML parser or copying structural rows at
publication. Property-element statement reification now reaches complete graph
construction, while the strict OWL mapper remains fail-closed for its classic
metadata. This additional syntax coverage does not advertise `parse-rdfxml-v1`
or make RDF/XML a public routing target.

Node-element property attributes now follow the same bounded forward path.
`rdf:type` attribute values resolve against the active XML base, ordinary
property attributes become exact language or `xsd:string` literals, and
`xml:lang=""` correctly clears an inherited language. Reserved RDF/XML syntax
terms remain rejected rather than being misinterpreted as predicates.

Empty property elements can now describe their selected object in the same
scan. `rdf:resource`, `rdf:nodeID`, and implicit blank objects receive exact
attribute-form type and literal edges before the outer property edge is
published. IRI/base and language semantics match node property attributes;
literal and triple limits remain live. Statement reification shares the graph
path described below; the legacy datatype-plus-property-attribute branch stays
fail-closed.

Container membership properties now expand `rdf:li` to node-local `rdf:_1`,
`rdf:_2`, and subsequent predicates in document order, including inside
implicit `parseType="Resource"` nodes. Expanded element IRIs and generated
membership IRIs are bounded, counter overflow fails before graph publication,
and reserved core/withdrawn RDF names are rejected in invalid node, property,
attribute, and root roles.

`rdf:parseType="Literal"` properties now stream complete XML fragments into
exact `rdf:XMLLiteral` terms. The retained lexical form matches Python's
ElementTree serialization for namespace declarations, attributes, nested and
empty elements, mixed text, and child tails; decoded entities and CDATA remain
in document order. Inherited language is suppressed, literal limits stay live
during serialization, and XInclude is still forbidden. XML-literal statement
reification now reaches complete graph construction. Every other `parseType`
value follows the same XML-literal production.

The forward XML scanner now accepts the complete XML 1.0 Unicode name ranges
for element, attribute, and namespace-prefix names. Qualified names still have
strict namespace structure: multiple colons and malformed namespace
declarations fail before graph publication, as does an empty property-element
`rdf:nodeID`. Unicode annotation names cross the same retained owner with exact
Python mapping and wire parity.

XML 1.0 value normalization now also happens in the forward scan. Literal text
and CDATA normalize raw CR and CRLF endings to LF, attribute values normalize
raw XML whitespace to spaces, and numeric character references retain their
referenced characters. The same event stream feeds nested XMLLiteral
serialization, including ElementTree-compatible escaped attribute whitespace.
Forbidden raw XML characters and a literal `]]>` in character data fail before
graph or owner publication.

The XML envelope is now validated before RDF parsing. XML declarations require
the ordered `version`, optional `encoding`, and optional `standalone`
pseudo-attributes; only encodings compatible with the owned UTF-8 source are
accepted. Reserved `xml`/`xmlns` namespace bindings and prefixed namespace
undeclarations follow the namespace constraints, while an explicit correct
`xml` binding and default-namespace undeclaration remain valid. The positive
envelope crosses retained publication; malformed forms publish no graph or
owner.

Property-element `rdf:ID` now emits the original statement plus the complete
four-triple RDF 1.1 reification for literal, resource, nested-node,
`parseType="Resource"`, empty and nonempty collection, and XML-literal forms.
The reification IRI follows the active XML base. `rdf:ID` and `rdf:nodeID`
values enforce the XML NCName grammar, including Unicode names, and the
document-wide (`rdf:ID` value, base) uniqueness constraint spans node and
property elements. The ID ledger, object copies, and duplicate checks remain
under native memory/work limits, while every emitted statement remains under
`max_triples`. Classic `rdf:Statement` metadata is still unconsumed by the
strict OWL reverse mapper, so a reified graph fails before retained-owner
publication and makes no capability claim.

RDF/XML 1.1 `parseTypeOther` values now behave exactly like
`rdf:parseType="Literal"` in both the Python reference parser and native forward
scanner. Arbitrary values, nested markup, mixed tails, suppressed inherited
language, XML-literal limits, and optional `rdf:ID` reification reuse the same
bounded event/serialization path without emitting value-specific triples. A
fully mappable annotation fixture crosses the private retained V2 owner with
exact Python canonical, fingerprint, report, wire, and zero-structural-copy
parity.

## Implemented checkpoint

- Commit `b1e802e` introduced the guarded public-load promotion into a typed V2
  owner for the eligible single-document Functional shape.
- Commit `8c43fb2` retained exact canonical origin rows in the shared immutable
  owner, including the zero-origin edge, and rejected unsupported provenance
  shapes before publication.
- Commit `677084d` sealed the exact raw/effective structural alias only after
  proving equality, then made PYOCORE encoding consume one direct-column
  publication plus one retained-origin page. Missing attested direct columns
  fail with `NATIVE_WIRE_SOURCE`; the path cannot fall back to scalar model
  traversal.
- Commit `4216f23` moved typed-arena construction into the Functional parser
  operation. It carries one opaque native storage owner to the attested
  publication boundary, bypasses the legacy Python-row retention bridge,
  records the exact parser byte count, and exposes native syntax-parse,
  result-framing, arena-construction, freeze, and root-parse timings.
- Commit `bdbaac9` made origin attachment attestation-driven, so the same
  parser-built owner works with provenance enabled or disabled. The disabled
  path retains zero origin rows/bytes, advertises no origin capability, and
  avoids Python provenance matching/construction.
- Commit `47ec85f` retained the parser-built owner when the existing AUTO policy
  selects native parsing for a large Functional source. Small AUTO documents
  stay Python-backed without invoking the retained parser. V2 publication
  accepts AUTO only with native parser provenance, and an ignored direct-import
  edge now has exact Python/native manifest and wire coverage.
- Commit `81391c1` moved publication ahead of complete Functional-model decode
  for the eligible no-anonymous-node shape. A fail-closed canonical byte scanner
  validates the parser framing and derives the exact attestation inputs while
  reporting parser-result bytes, canonical rows, structural occurrences,
  published roots, metadata IRIs, provenance occurrence records, and zero eager
  structural objects. Anonymous individuals select the prior authoritative
  decode-and-re-scope path exactly once. A sealed empty anonymous-scope result
  also prevents composite consumers from triggering scalar scope paging.
- Commit `927cf89` removed the remaining proportional Python publication seam
  for the guarded path. The parser returns a compact seed; native preparation
  streams retained canonical roots into exact document/root manifests and
  structural, logical, and signature fingerprints; origin rows stay native;
  and counters attest zero canonical/preimage/provenance bytes or objects in
  Python. Native logical work has fallible allocation, cancellation checkpoints,
  and explicit temporary/owner memory accounting.
- Commit `4030c80` extended owner-first eligibility to single-document
  `RECORD_UNRESOLVED` loads when no resolver is supplied. Import edges,
  diagnostics, IRI reference kinds, warnings, resolution attempts,
  fingerprints, and wire output match the Python path. A pre-publication
  diagnostic-budget check selects complete framing when the retained
  attestation could not represent the configured limit.
- Commit `530a696` closed the production and test-hook Clippy gate for the new
  retained preparation helpers without widening any capability claim.
- Commit `1dde7dc` exposed already-validated retained origin records as a
  private streaming seam. The PYOCORE writer now consumes those exact typed
  records without decoding each native auxiliary row a second time, caching a
  facade page, or incrementing Python materialization counters.
- Commits `34006f3` and `1b26f35` extended owner-first publication to empty
  Functional closures under `RESOLVE_LOCAL` and `RESOLVE_STRICT`. The native
  parser selects complete framing before publication when either policy has a
  direct import, so resolver-backed closure orchestration cannot be bypassed.
- Commit `f667e72` added installed-wheel parity for both empty-closure policies
  and updated consumer instrumentation to distinguish Python parser dispatches
  from retained native parsing.
- Commits `a1bae4c` and `ff14299` retain an exact compact common-contract
  summary beside the owner and bind the schema-v2 prepared bytes to native
  finalization. Four fingerprint preimage lengths/digests, four record
  inventories, and exact root/node counts are computed while Rust already
  streams canonical roots; altered summary bytes cannot be substituted before
  publication.
- Commits `e3b0155` and `4608cc4` make the native comparator consume that one
  frozen summary and the retained-origin stream without requesting a second
  encoded DAG. Exact scalar parity, type/schema/count/digest tamper rejection,
  immutability/lifetime, direct-column count parity, and zero-view/zero-facade
  counters are covered.
- Commit `653a4c7` streams RDF/XML `rdf:parseType="Resource"` properties into
  one implicit blank node in the forward parser. Differential tests prove exact
  Python canonical axioms and mapping counts, while the private production seam
  proves fingerprint, lazy RDF report, wire, and retained-owner parity with zero
  structural publication copies. Triple limits are enforced during the graph
  sink, and unsupported parse-type/reification forms still fail closed.
- Commit `e057b5c` maps RDF/XML node property attributes directly during the
  forward scan. It resolves attribute-form `rdf:type`, preserves inherited and
  explicitly cleared language state, enforces literal limits before graph
  insertion, and rejects reserved syntax attributes. Differential and private
  retained-owner tests prove exact canonical, fingerprint, report, and wire
  parity without structural publication copies.
- Commit `912ea42` maps RDF/XML empty-property-element attributes onto IRI,
  named blank, and generated blank objects. It preserves base/language
  semantics, rejects reserved syntax and unsupported reification forms, and
  keeps datatype/property-attribute ambiguity outside the closed slice.
  Differential and retained-owner tests prove exact Python canonical values,
  fingerprints, RDF reports, wire output, limits, and zero structural
  publication copies.
- Commit `20bba35` implements the normative node-scoped `rdf:li` expansion and
  validates RDF/XML element roles against core, description, container, and
  withdrawn syntax terms. A fully mappable annotation-property fixture proves
  exact Python canonical values, fingerprints, RDF reports, wire output,
  limits, and retained-owner publication with zero structural copies.
- Commit `fc8e75d` streams markup-free `rdf:parseType="Literal"` content as an
  exact `rdf:XMLLiteral`, preserving decoded text/CDATA order while suppressing
  inherited language. Differential and retained-owner tests prove canonical,
  fingerprint, RDF-report, wire, and zero-copy parity; nested markup and
  reification remain excluded rather than being flattened or ignored.
- Commit `7fad496` extends that path to nested XML fragments with
  ElementTree-compatible namespace allocation, attribute ordering and
  escaping, mixed content and tails, multiple child roots, and empty-element
  spelling. The parser captures fragment events without applying semantic
  XML-base/language resolution inside the literal, enforces the literal-byte
  limit while serializing, and keeps XInclude and reification fail-closed.
  Differential and private retained-owner tests prove exact canonical,
  fingerprint, RDF-report, wire, and zero-structural-copy parity.
- Commit `c283268` replaces the scanner's ASCII-only XML-name restriction with
  the XML 1.0 Unicode `NameStartChar`/`NameChar` ranges. Unicode namespace
  prefixes, element names, and qualified attributes now expand and map exactly;
  multi-colon QNames, malformed namespace declaration names, and empty
  property-element `rdf:nodeID` values fail as syntax. Differential and private
  retained-owner tests preserve canonical, fingerprint, RDF-report, wire, and
  zero-structural-copy parity.
- Commit `48470cb` applies XML 1.0 end-of-line and attribute-value
  normalization during the bounded forward scan. Text, CDATA, ordinary
  attributes, and nested XMLLiteral attributes distinguish raw whitespace from
  numeric references exactly like ElementTree. Forbidden raw characters and
  literal CDATA-close sequences in character data fail before graph
  publication. Differential and private retained-owner tests preserve exact
  canonical, fingerprint, RDF-report, wire, and zero-structural-copy parity.
- Commit `c812c23` validates the XML declaration grammar, pseudo-attribute
  order, UTF-8/ASCII-compatible encoding selection, standalone values, and
  reserved namespace bindings before RDF parsing. A valid declaration,
  explicit `xml` binding, and default namespace undeclaration cross the private
  retained owner with exact Python canonical, fingerprint, RDF-report, wire,
  and zero-structural-copy parity; malformed or forbidden envelopes publish no
  owner.
- Commit `3bd19e3` implements property-element `rdf:ID` statement reification
  for every supported graph production. It emits the original edge and exact
  `rdf:type`, `rdf:subject`, `rdf:predicate`, and `rdf:object` metadata, retains
  collection heads and literal identity, resolves IDs against the active base,
  and enforces Unicode NCNames plus document-wide ID/base uniqueness. Triple
  and work limits remain live. The strict OWL mapper deliberately rejects the
  unconsumed classic reification metadata, so no retained owner or public
  capability is inferred from this graph-syntax checkpoint.
- Commit `1e51013` makes every nonstandard `rdf:parseType` value follow the
  RDF/XML 1.1 XML-literal production in both the Python reference parser and
  native scanner. Nested markup and `rdf:ID` reuse the existing bounded literal
  and reification paths. Differential and private retained-owner tests prove
  exact canonical, fingerprint, RDF-report, wire, limit, and
  zero-structural-copy parity without changing capability ledgers.
- Anonymous-node re-scoping, resolver-backed or resolved import closure, source
  maps, OWL 2 DL validation, RDF-backed formats, and any raw/effective row
  mismatch remain on the complete Python snapshot path.
- `INGESTION_FEATURES`, `VIEW_FEATURES`, and
  `CoreCapabilities.encoded_view_schemas` remain empty. The existing
  `parse-functional-v1` foundation capability describes document parsing; it
  is not a claim that the WP16 retained-ingestion matrix is complete.

## Deliverable ledger

| WP16 deliverable | Status | Evidence / blocker |
|---|---|---|
| Streaming RDF/XML plus native OWL 2 RDF mapping | partial private production slice | The all-feature local suite exercises the private slice's growing mapping surface, security framing, limits, cancellation, and typed V2 adapter. Standard `rdf:parseType="Resource"` structural expressions, node and empty-property-element property attributes, node-local `rdf:li` expansion, complete nested `parseType="Literal"`/`parseTypeOther` XML-fragment serialization, XML 1.0 Unicode QNames and value normalization, and strict XML declaration/namespace envelopes now cross the forward parser and retained V2 owner exactly. Property-element `rdf:ID` is graph-complete across those productions, including NCName/uniqueness constraints, but its classic reification metadata remains outside strict OWL owner publication. It is not a complete RDF/XML parser or mapper, is not a public routing target, and is not advertised. |
| Turtle, OWL/XML, and Functional parsers | Functional parser plus owner-first retained checkpoint | `parse-functional-v1` has constructor, generated, malformed-input, limit, cancellation, and differential tests. Its guarded no-anonymous-node path constructs the arena and prepares publication without complete parser framing or proportional Python structural bytes. Turtle and OWL/XML have no complete native path. |
| Native header/import/annotation/list/blank-node/rule mapping | partial | Functional parsing covers the generated constructor surface and selected SWRL/anonymous cases. The RDF/XML slice maps collection and `parseType="Resource"` blank nodes directly for structural expressions. Ignored imports, no-resolver record-unresolved imports, and empty direct-import sets under both resolver policies cross retained publication with exact manifest parity. Anonymous-individual re-scoping remains excluded. Complete resolved-import and cross-format mapping remains open. |
| Direct parser-to-arena freeze | guarded Functional slice passes owner-first handoff | The Functional parser feeds canonical roots into `TypedFacadeBuilderV2`, freezes the typed owner, and later streams canonical temporaries internally for publication. Python supplies neither the owner roots nor an ontology-sized canonical/preimage/origin collection. End-to-end multi-format and resolved-closure acceptance remains open. |
| Diagnostics, source positions, limits, and cancellation | partial | Functional parser error/limit/cancellation behavior is covered. No-resolver unresolved imports preserve warning, suppression, edge diagnostic, and IRI-reference semantics; an insufficient diagnostic budget selects the complete fallback before retained publication. Source maps remain excluded and complete per-format diagnostic/source-position coverage is open. |
| Retained native import closure | empty closure plus single-document unresolved checkpoint | An empty direct-import set retains one native root owner under every import policy. Ignored imports and no-resolver `RECORD_UNRESOLVED` edges also retain that owner. Actual resolver-backed imports select Python document/closure orchestration before publication. Diamonds, cycles, shared digest parsing, partial failure, cancellation, and deterministic bounded parallel multi-owner publication remain open. |
| Fail-closed capability routing | checkpoint pass for guarded slice | Ineligible shapes skip owner construction; eligible owner-construction failure propagates without fallback; missing attested direct wire columns fail closed. Small AUTO loads never invoke the retained parser, and V2 rejects AUTO publication without native parser provenance. The successor capability ledgers remain empty. |
| Installed-wheel forced-native matrix | guarded Functional matrix | A fresh isolated wheel passes provenance enabled/disabled, both empty resolver-policy closures, large AUTO with an ignored import, and no-resolver `RECORD_UNRESOLVED`. It proves compact summaries, zero Python canonical/preimage/provenance materialization, exact diagnostics/manifests/wire, direct/wire/decode/mmap/lifetime parity, and fallback for anonymous/tight-diagnostic shapes. This is not the required every-format/option/negative/import matrix. |
| Differential, W3C, corpus, round-trip, hostile, fuzz, memory, and source-map evidence | partial local Functional and RDF/XML evidence | Local Functional differential/generated/hostile tests pass. Focused RDF/XML graph, Python differential, limit, retained publication, report, and wire tests pass; graph-only `rdf:ID` cases prove exact reification and strict no-owner failure. W3C, representative external corpora, retained cross-layer corpus breadth, fuzz, memory/object proof, and complete source-map evidence remain open. |
| Phase/object/allocation reporting | partial | Publication counters now distinguish compact summary bytes, zero Python canonical/preimage copies, native canonical rows/bytes encoded, native fingerprint workspace, native origin rows/bytes, structural occurrences/roots, metadata IRIs, retained storage, facade pages/rows, encoded-view requests, and Python model materialization. Timings add native publication preparation. Mapping, closure, and complete per-phase allocation evidence remain open. |

## Verification evidence

At commit `f667e72`, a fresh native wheel was built and installed into
`/private/tmp/pyowlcore-empty-closure.M4uN3C/venv`. The isolated
runner and focused public/consumer matrix reported:

```text
PYTHONPATH=/private/tmp/pyowlcore-empty-closure.M4uN3C/venv/lib/python3.12/site-packages:. \
PYOWL_CORE_TEST_NATIVE_LIBRARY=1 \
.venv/bin/python -m pytest -q \
  tests/native/formats/test_public_retained_load.py \
  tests/integration/native_consumers/test_retained_view_matrix.py

22 passed in 3.14s
```

The installed runner proved:

- `parser_result_bytes_scanned=0`, a 528-byte combined compact/prepared summary,
  zero canonical bytes copied to Python, zero fingerprint-preimage bytes in
  Python, zero Python provenance occurrence records, and zero eager structural
  objects;
- five canonical rows/239 canonical bytes streamed internally for publication,
  three native origin rows/432 retained origin bytes, and exact owner/wire
  counters;
- byte-identical Python and retained PYOCORE images with SHA-256
  `dd92dee2b78189486e928569257e0fae7115726209373486513adc6be1402ec4`;
- exact structural/logical/signature/origin/direct/decode/mmap parity, one shared
  direct exporter, one read-only mmap exporter, expected close blocking, and
  surviving borrowed buffers after owner close;
- provenance-disabled parity with no origin capability/table and large AUTO
  ignored-import parity;
- owner-first `RECORD_UNRESOLVED` parity for manifest, IRI-typed diagnostics,
  warnings, resolution attempts, fingerprints, and wire output;
- owner-first `RESOLVE_LOCAL` and `RESOLVE_STRICT` parity for an empty direct
  import set, with zero resolution attempts and zero complete parser-result
  bytes; and
- already-validated native origin records decoded once per emitted row, with
  one page request and no Python facade cache or materialization-counter change.

At revision `4608cc4`, a fresh production wheel was installed into
`/private/tmp/pyowlcore-summary-wheel.vkgOxW/venv`. The wheel SHA-256 is
`8ae5146eb2b03c5514d328fd21c7df95426666bc0cd83c4ca377d76e4c8b1045` and
its extension SHA-256 is
`a62130c3d548b17b404a80aec67dc929076563e1e6d1ccef59ec991801faed7b`.
The fresh/steady resident-byte comparator smoke passed all four Python/native
common-contract equality assertions with no execution error. Both native
samples reported one retained summary, zero encoded views/referenced buffers,
15 provenance rows, 31 nodes, 15 roots, and zero scalar traversal or structural
materialization. The native common-adapter portions were 3.66 ms fresh and
1.98 ms steady in this single-repetition shared-host diagnostic.

A separate installed-wheel 5,000-class Functional diagnostic (9,999 axioms,
19,999 nodes, 944,938 source bytes) completed three retained contract builds in
0.412, 0.414, and 0.476 seconds after loads of 0.699, 0.682, and 0.688 seconds.
Each run consumed one summary and 9,999 retained provenance rows with zero
views, referenced structural bytes, model-row delta, or auxiliary-row delta.
These timings are algorithmic checkpoint evidence, not an approved WP18 ratio
gate.

The comparator suite passed 159 tests. The compact-summary focused gates passed
five production parity tests, one installed-runner test, and six test-hook
tests. The earlier Rust test configuration passed 188 tests; production and
test-hook Clippy passed with warnings denied; and the explicit release-native
suite passed 319 tests plus 3068 subtests with 28 expected private-hook skips.
Ruff passed for the changed source and tests. The source retained/consumer
matrix passed 21 tests with only the installed isolation wrapper excluded, and the focused
consumer regression plus both empty-closure cases passed three tests. A
concurrent Python unit/integration run initially reached 241 passes plus 356
subtests with 13 failures: the one WP16 parser-accounting expectation is fixed
and passes in the focused rerun; the other 12 failures are the shared tree's
concurrent mapped-buffer close lifecycle work and are outside this slice. No
external W3C/corpus, sanitizer, fuzz, or approved-machine result is claimed.

At commit `653a4c7`, the isolated no-default-feature Rust suite passed all 199
tests, including three focused `parseType` scanner tests and 76 RDF ingestion/
mapping tests. The complete focused RDF/XML ingestion and retained-V2 Python
files passed 26 tests against a freshly rebuilt test-hook extension. All-feature
Clippy passed with warnings denied; Rustfmt and Ruff checks also passed. This is
local differential and retained-owner evidence only: no installed-wheel or
public capability claim is inferred from the private seam.

At commit `e057b5c`, that isolated Rust suite passed 201 tests and the two
focused RDF/XML Python files passed 28 tests. The added differential cases
cover attribute-form types, language literals, empty-language inheritance
reset, reserved syntax rejection, and literal limits; the private retained
case again proves exact report/wire output and zero structural publication
copies. All-feature Clippy, Rustfmt, and Ruff remained clean.

At commit `912ea42`, the isolated Rust suite passed 203 tests and the two
focused RDF/XML Python files passed 30 tests against a rebuilt test-hook
extension. The new graph cases cover IRI, named-blank, and generated-blank
objects, resolved attribute-form types, language literals, reserved syntax,
unsupported reification/datatype forms, and explicit empty-element spelling.
Python differential and private retained-owner cases prove exact canonical,
fingerprint, RDF-report, and wire parity plus zero structural publication
copies; literal and triple limits fail before publication. All-feature Clippy,
Rustfmt, and Ruff passed with warnings denied.

At commit `20bba35`, the isolated Rust suite passed 206 tests and the two
focused RDF/XML Python files passed 33 tests against a rebuilt test-hook
extension. Graph tests prove document-order, node-local `rdf:li` numbering,
independent implicit-node counters, overflow behavior, reserved element-role
rejection, and root-attribute rejection. The Python differential and private
retained-owner cases use declared `rdf:_1`/`rdf:_2` annotation properties to
prove exact canonical, fingerprint, RDF-report, and wire parity with zero
structural publication copies; triple limits and invalid element roles fail
before owner publication. All-feature Clippy, Rustfmt, and Ruff passed with
warnings denied.

At commit `fc8e75d`, the isolated Rust suite passed 208 tests and the two
focused RDF/XML Python files passed 36 tests against a rebuilt test-hook
extension. Text, decoded entity, CDATA, empty content, inherited-language
suppression, literal-limit, nested-markup, and reification cases are covered.
The Python differential and private retained-owner tests prove exact canonical,
fingerprint, RDF-report, and wire parity with zero structural publication
copies; unsupported nested markup publishes no owner. All-feature Clippy,
Rustfmt, and Ruff passed with warnings denied.

At commit `7fad496`, the isolated no-default-feature Rust suite passed all 209
tests and the two focused RDF/XML Python files passed 38 tests against a fresh
test-hook build. The added cases cover arbitrary and known namespace prefixes,
namespace declaration ordering, attributes, nested and empty elements, mixed
text and tails, multiple child roots, escaping, opaque nested `xml:base`, the
literal-byte limit, and forbidden nested XInclude. Python differential and
private retained-owner tests prove exact canonical, fingerprint, RDF-report,
wire, and zero-structural-publication-copy parity. All-feature Clippy passed
with warnings denied; Rustfmt and Ruff remained clean.

At commit `c283268`, the isolated no-default-feature Rust suite passed all 211
tests and the two focused RDF/XML Python files passed 42 tests against a fresh
test-hook build. Positive cases cover Unicode namespace prefixes, element
names, qualified attributes, language literals, decoded-codepoint accounting,
literal limits, and retained owner publication. Negative cases reject
multi-colon QNames, malformed namespace declaration names, and empty
property-element `rdf:nodeID` before publication. Python differential and
private retained-owner tests prove exact canonical, fingerprint, RDF-report,
wire, and zero-structural-publication-copy parity. All-feature Clippy passed
with warnings denied; Rustfmt and Ruff remained clean.

At commit `48470cb`, the isolated no-default-feature Rust suite passed all 213
tests and the two focused RDF/XML Python files passed 46 tests against a fresh
test-hook build. Positive cases cover raw CR/CRLF normalization in text and
CDATA, raw whitespace normalization in attributes, preservation of numeric
CR/LF/tab references, nested XMLLiteral serialization, literal limits, and
retained owner publication. Negative cases reject forbidden raw XML characters
in text, CDATA, and comments plus literal `]]>` character data before
publication. Python differential and private retained-owner tests prove exact
canonical, fingerprint, RDF-report, wire, and zero-structural-publication-copy
parity. All-feature Clippy passed with warnings denied; Rustfmt and Ruff
remained clean.

At commit `c812c23`, the isolated no-default-feature Rust suite passed all 214
tests and the two focused RDF/XML Python files passed 51 tests against a fresh
test-hook build. Positive cases cover a complete declaration with whitespace,
UTF-8 encoding and standalone mode, explicit correct `xml` binding, default
namespace undeclaration, exact mapping, and retained owner publication.
Negative cases cover missing or unordered declaration attributes, invalid
standalone values, unsupported encodings, reserved namespace aliases, and
prefixed undeclarations. Python differential and private retained-owner tests
prove exact canonical, fingerprint, RDF-report, wire, and
zero-structural-publication-copy parity. All-feature Clippy passed with warnings
denied; Rustfmt and Ruff remained clean.

At commit `3bd19e3`, the isolated no-default-feature Rust suite passed all 217
tests and the two focused RDF/XML Python files passed 55 tests against a fresh
test-hook build. Direct graph cases cover language and datatyped literals,
resource and nested-node objects, property-attribute blank objects,
`parseType="Resource"`, empty and nonempty collections, and nested XML
literals. Every form proves the original edge plus all four classic reification
triples. Positive constraints cover active-base resolution, Unicode NCNames,
and reuse of one ID under distinct bases; negative cases reject empty or
malformed NCNames and duplicate ID/base pairs spanning node and property
elements. Triple-limit failure and strict mapping failure occur before owner
publication. All-feature Clippy passed with warnings denied; Rustfmt, Ruff, and
diff checks remained clean. This is graph-syntax evidence only and does not
claim retained-owner or public RDF/XML capability coverage for reification.

At commit `1e51013`, the isolated no-default-feature Rust suite remained at 217
passing tests, the two focused RDF/XML Python files passed 57 tests against a
fresh test-hook build, and all 15 parsing-format unit tests passed. Graph tests
prove that a nonstandard `parseType` produces the exact nested XML literal and
can be reified with `rdf:ID`; Python fallback tests prove the same RDF/XML 1.1
semantics. Differential and private retained-owner cases prove exact canonical,
fingerprint, RDF-report, wire, literal-limit, and zero-structural-copy parity.
All-feature Clippy passed with warnings denied; Rustfmt, Ruff, and diff checks
remained clean. The RDF/XML capability ledgers remain empty.

## Acceptance state

WP16 acceptance remains open. In particular:

- the guarded Functional path now constructs and publishes its retained arena
  without eagerly decoding a complete Python structural model or copying
  proportional canonical/preimage/origin data into Python; anonymous-node
  re-scoping and diagnostic-budget fallback still use complete-model decode;
- RDF/XML, Turtle, and OWL/XML are not complete native ingestion paths, and the
  full Functional format/option/import matrix has not passed retained
  publication despite AUTO, all-policy empty-closure, ignored-import, and
  no-resolver unresolved-import coverage;
- native closure assembly does not yet prove shared-import digest parsing,
  diamonds/cycles, anonymous scopes, partial failure, cancellation, or
  deterministic bounded parallel publication;
- complete diagnostics/source-position, writer, W3C/corpus, hostile, fuzz,
  memory/object, and phase-allocation evidence is absent; and
- no ingestion format or encoded schema may be advertised from this checkpoint.

WP16 can close only after each advertised format completes the installed-wheel
forced-native matrix, unsupported selections fail before consumption, fallback
detection remains active, and parser-to-arena plus retained-closure evidence
demonstrates that no RDFLib graph, proportional Python triple/axiom collection,
or eager complete Python model conversion occurs.
