# WP14 — retained-native contract and comparator baseline

Status: design handoff, paired scheduler, executable gates, persistent
lifecycle, direct retained-Rust and raw/common Horned runners, and
installed-native adapter checkpoint recorded; comparative baseline incomplete
Prepared: 2026-07-20
Implementation/release claim: none

## Outcome

WP14 now has a written retained-storage/API decision and normative comparative
timing fence in
`reports/performance/redesign-baseline/decision.md`. The decision freezes the
private-handle ownership model, lazy scalar facade, encoded structural schema
direction, mmap ownership, feature-negotiation rules, and the disjoint seams
WP15 hands to WP16/WP17.

This is a partial WP14 handoff, not WP14 acceptance. The fail-closed comparator
manifest, core Python common-contract adapter, seeded paired-block scheduler,
executable ratio gates, and audited steady-process lifecycle are implemented.
The checked-in core-only resident-byte fresh/steady smoke is historical and
predates the scheduler/gates/lifecycle. A second clean-commit py-horned smoke
now exercises three paired repetitions in every fresh/steady and resident/file
combination. A third clean-commit raw Horned smoke exercises that same tiny
matrix. A fourth clean-commit smoke runs the shared raw v2/common v1 Horned
executable across all four modes, with 12 passing Python/common and
resident/file equality assertions and clean persistent lifecycles for both
boundaries. A fifth clean-commit smoke runs the direct retained-Rust v1
executable across Functional Syntax and RDF/XML in all four modes, with 24
passing equality assertions and a clean 16-request persistent lifecycle. All
succeed only with the explicit `--allow-partial` opt-in, and
the default runner exit remains fail-closed.
One external runner artifact and approved reference-machine evidence,
representative approved-machine samples, and complete retained
materialization/copy measurements remain open. The raw Horned and py-horned
lanes, the independent common-contract Horned lane, and the direct retained-Rust
lane are complete and exact-artifact/SHA bound. Local development checks pass
the direct lane on Functional Syntax and RDF/XML and both Horned boundaries
across Functional Syntax, OWL/XML, and RDF/XML in fresh/steady and
resident/file modes and pass py-horned's common contract in the same modes;
these are not approved evidence.
A local isolated installed-native adapter smoke now
passes the common contract in fresh and steady resident-byte modes, but it is
not the required comparative matrix or approved-machine evidence. The source
and pure-artifact exclusion audit is recorded, but native-wheel linkage,
SBOM/license review, and release packaging remain open. No `not-run` lane is
treated as a pass, and the configured gates remain failed while their required
evidence is unavailable.

## Deliverable ledger

| WP14 deliverable | Status | Evidence / blocker |
|---|---|---|
| Retained storage/API decision | recorded | `reports/performance/redesign-baseline/decision.md` sections 1–5 |
| Exact comparative timing fences | scaffold implemented | `benchmarks/comparators/comparators.toml` and executable validation cover all 17 phases. Python retained-handle publication is `n/a`; only its ordinary snapshot/common-ledger completion is timed. |
| Current implementation limitation inventory | recorded, unmeasured | Decision section 7; no numeric object/copy claim is made. |
| WP15 frozen seams | recorded | Decision section 10 |
| Comparator manifest/pin scaffold | recorded, not approved evidence | Engine/package policies, allocator/thread/JVM settings, corpus-manifest link, phase fences, and pending machine key are recorded. Every external lane has a separate runner pin. Direct Rust, raw/common Horned, and py-horned are complete and SHA-256 bound; OWLAPI remains pending and non-runnable. |
| External comparator runners | direct Rust, raw/common Horned, and py-horned complete; OWLAPI pending | One authenticated, bounded, versioned persistent process per steady lane is implemented and adversarially contract-tested, including fresh ontology-instance evidence and deterministic shutdown. Direct runner v1 links the native crate as an `rlib`, verifies its embedded native Cargo lock and executable hash, uses retained Functional and streaming RDF/XML storage without Python/PyO3 traversal, and passes exact Python common-contract differential checks in resident/file and fresh/steady modes; OWL/XML and Turtle fail closed as ineligible. `shared-host-direct-smoke.json` binds it to clean commit `588853e`, with 24 passing assertions and a clean 16-request lifecycle. `shared-host-py-horned-smoke.json` binds py-horned runner v2 and its exact 1.4.0 sdist to clean commit `3315c22`, with all 12 selected common-contract assertions passing. `shared-host-horned-raw-smoke.json` preserves historical raw v1 evidence at clean commit `f6845ec`. `shared-host-horned-common-smoke.json` binds the shared raw v2/common v1 executable, crates.io checksum, embedded Cargo lock, Rust 1.97.1 toolchain, and executable hash to clean commit `f12a4f1`; each persistent lane serves eight requests with distinct identities, empty stderr, and clean shutdown, and all 12 selected common-contract assertions pass. Exact local Horned Functional, OWL/XML, RDF/XML, SWRL, anonymous-node, import, annotation, and all-supported-constructor differential checks pass; Turtle and parser surfaces that lose nested annotations are explicitly ineligible. |
| Installed-wheel bulk adapter | local checkpoint pass; acceptance matrix not-run | A fresh CPython 3.12 macOS x86-64 wheel at core revision `4608cc4` passed Python/native common-contract equality in fresh and steady resident-byte modes. The retained lane consumed one compact native summary and 15 streamed provenance rows with zero encoded views, referenced structural buffers, scalar traversals, or structural-node materializations. File modes, repetitions, representative corpora, other platforms/Pythons, and approved-machine evidence remain open. |
| Core Python fresh/steady raw samples | development smokes only | Historical `shared-host-smoke.json` contains one generated Functional Syntax resident-byte repetition in each process mode. The py-horned, historical raw Horned, and shared raw/common Horned smokes each add three paired Python/external repetitions in every fresh/steady and resident/file combination. The direct smoke covers the same matrix for tiny Functional and medium RDF/XML. None is approved performance evidence. |
| File-lane execution boundary | implemented, approved evidence pending | Core fresh/steady file tests hash-check and prepare bytes before timing, bind the same source-derived document IRI as resident bytes, time file open/read, record temporary bytes, and produce the same common-contract digest. Committed direct, raw/common Horned, and py-horned smokes pass the same resident/file lifecycle fence; no approved-machine file result exists yet. |
| Paired randomized execution | implemented, evidence pending | An exact recorded u64 seed drives a stable per-scenario/per-block SHA-256 lane permutation. Raw samples retain seed, paired block, order, and block size; steady warm-ups and out-of-timer cleanup are equal across selected lanes. The historical committed smoke predates this scheduler. |
| Executable comparative gates | implemented, not passed | Resident-byte fresh/steady gates pair direct Rust with Horned common and installed wheel with py-horned common. Fresh wall gates use parent startup-to-ready measurements; steady wall and installed-wheel/direct overhead gates use call-to-ready measurements. A versioned SHA-256 counter/rejection stream drives stratified paired bootstrap, which aggregates required medium/large corpus medians geometrically. Wall/RSS upper 95% bounds are fixed at 1.10/1.15, every required large median at 1.25, and installed-wheel/direct median call overhead at 1.15. Missing/invalid/nonpositive evidence fails with scenario reasons, and raw Horned is excluded. The missing OWLAPI runner and approved paired matrix mean current gates do not pass. |
| Representative randomized samples and profiles | not-run | The scheduler is implemented, but medium/list-heavy and large RDF/XML paired samples and complete phase profiles have not run on an approved machine. |
| NCIT/DOID and representative medium/large phase profiles | not-run on approved reference machine | Instrumentation and approved machine run are not recorded. |
| No-Horned/no-Java distribution audit | partial pass | `dependency-audit-shared-host.json` binds alias-aware source and packaged-Python scans plus reproducible SHA-bound sdist/pure-wheel inspection to clean commit `f89c9d0`. A later SHA-bound native-wheel static audit also passed with no forbidden dependency finding. Platform linkage is explicitly `not-run`; release SBOM/license review and approved release packaging remain open. |

## Decisions frozen for implementation

- Public documents/snapshots own opaque private handles; a handle owns its
  immutable arena or retains the immutable validated mapping owner.
- Dependent scalar values, indexes, overlays/composites, and encoded views keep
  the required owner alive; no unowned pointer crosses calls.
- Native load/freeze creates only O(documents + diagnostics + fixed facade
  metadata) Python objects before scalar access. Scalar values are lazy and
  caches cannot permanently duplicate the ontology.
- `EncodedStructuralView` is requested through the public `OntologyView.view`
  boundary, uses core-owned read-only little-endian buffers and canonical
  descriptor metadata, and retains its exact originating view as owner.
- WP17, not WP14/WP15, freezes schema 1 and any API/adapter version change.
  Until then the encoded schema is not advertised.
- PYOCORE remains the sole stable cross-process format. mmap publication owns
  one validated immutable mapping and performs neither row-wise Python
  materialization nor a complete native copy.
- Consumers negotiate `CoreCapabilities`, especially
  `retained-native-load-v1`, `ontology-identity-index`, `wire-v1`,
  `wire-verified`, and
  `encoded_view_schemas["pyowl-core/structural-columns"]`. Package version
  strings and private extension imports are not negotiation mechanisms.
- Capability selection happens before input consumption. Unsupported `AUTO`
  operations use the complete Python path; forced `NATIVE` fails explicitly.

## Timing decision

Resident-byte timers include parsing/mapping, interning, canonicalization,
freeze, required indexes, four fingerprint preimages/digests, identity,
diagnostics/provenance, publication, and complete common-ledger traversal.
Only comparison of already-produced ledger bytes/counts/digests and bounded
sample validation occur after the timer. Acquisition/network/resolver I/O is
separate.

For the core Python lane, retained-handle/facade publication is `n/a`; the
ordinary Python snapshot and common-contract ledger still finish inside the
timer. The runner implements resident-byte and prepared-file fresh/steady
execution. It executes measured repetitions as paired blocks, uses a recorded
u64 seed to shuffle lane order inside each scenario/block, applies equal steady
warm-ups and cleanup barriers, and preserves seed/block/order metadata in every
raw sample. The committed smoke exercises only resident bytes and predates this
scheduler.

Raw `horned-model-ready` remains asymmetric diagnostic evidence. Only Horned
engine time plus the complete independent `common-contract-ready` adapter may
be used as the Horned-equivalence denominator. Direct Rust and installed-wheel
lanes, resident-byte and file lanes, and fresh- and steady-process modes are
reported separately.

## Installed adapter checkpoint

At core revision `4608cc4`, the production wheel
`pyowl_core-0.1.0.dev0-cp312-cp312-macosx_14_0_x86_64.whl` had SHA-256
`8ae5146eb2b03c5514d328fd21c7df95426666bc0cd83c4ca377d76e4c8b1045`;
the contained extension had SHA-256
`a62130c3d548b17b404a80aec67dc929076563e1e6d1ccef59ec991801faed7b`.
An isolated CPython 3.12 install passed all four selected Python/native fresh
and steady resident-byte common-contract assertions. The report remained
`comparative_complete=false` because external denominators, file modes,
representative corpora, and repetitions were deliberately not selected.

The static dependency audit passed the source and native-wheel checks, while
platform linkage remained explicitly `not-run`. Release artifact inspection
also remained fail-closed because approved repository/documentation/issue URLs
are not configured; this is the recorded release-owner metadata blocker, not a
reason to invent project URLs in this work package.

## Acceptance state

WP14 acceptance is open. There is no approved reference-machine evidence for
the redesign counters or comparator gates. Direct Rust, py-horned, and
raw/common Horned are complete external runner pins; OWLAPI remains pending
and non-runnable. The persistent lifecycle is implemented,
adversarially contract-tested, and exercised by committed direct, py-horned,
and both Horned-boundary development evidence, but it has no approved
representative matrix evidence. Paired scheduling and the fixed
executable ratio evaluators are implemented, but they fail closed because
required comparator samples are unavailable. File execution is contract-tested
in the core and exercised in the direct and py-horned lanes, but has no approved
external/reference-machine evidence. The
limited source/sdist/pure-wheel exclusion audit passed,
but native-wheel linkage, SBOM/license, and release-packaging evidence are
still incomplete. Therefore:

- no `<= 1.10` Horned equivalence result exists;
- no OWLAPI or fastest-implementation result exists;
- no retained object/copy/RSS or mmap-startup gate has passed;
- no threshold amendment is proposed; and
- `retained-native-load-v1` and the encoded structural schema remain
  unadvertised.

## Handoff

WP15 may proceed with the opaque handle, immutable arena, lazy facade,
lifetime, allocation-counter, generated/wire-input adapter, and fail-closed
binding seams recorded in the decision. WP15 must preserve the full Python
backend and cannot close WP14's comparative evidence by implementation alone.

WP14 closes only after every external runner pin and artifact is complete,
audited persistent runner processes execute the equivalent contracts, the
complete installed-native matrix runs in isolation, and approved
reference-machine fresh/steady and resident/file samples cover the
representative matrix. Paired randomized profiles, passing executable ratio
gates, current object/copy counters, native-wheel linkage, SBOM/license
evidence, and approved release packaging must also be committed and reviewed.
Until then documentation must describe the native backend as experimental
acceleration and must not claim Horned/OWLAPI parity or multi-consumer
zero-materialization performance.
