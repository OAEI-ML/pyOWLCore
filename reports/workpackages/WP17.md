# WP17 — encoded views, wire/mmap, and consumer handoff

Status: implementation checkpoint; candidate schema frozen and exercised,
capability promotion and release evidence open
Prepared: 2026-07-19
Capability claim: none

## Outcome

WP17 now has a generated, versioned structural-column contract and a public
`EncodedStructuralView` implementation for every structural constructor. The
same descriptor and independent decoder cover the complete Python producer,
retained native columns, mapped wire storage, and segmented overlay/composite
views. Direct retained and mmap-backed publications keep their immutable owner
alive and expose read-only buffers without scalar axiom traversal.

The candidate is deliberately unadvertised. The version decision remains
`frozen-unadvertised`, package/API/adapter/wire versions are unchanged, and
`CoreCapabilities.encoded_view_schemas` stays empty. Local source-tree tests
prove the contract and its fail-closed boundaries, including a real retained
pyELK owner fixture, but they are not the installed-wheel, sanitizer/fuzz,
approved-corpus, performance, and coordinated consumer-release evidence needed
to promote the schema.

## Frozen contract

- Schema name `pyowl-core/structural-columns`, version 1, has generated
  descriptor SHA-256
  `9ad29db6a7e616f65cea2957bc5ba8d1f9b99ef0eb1fe1432c09be25786267b5`.
- The descriptor binds all eleven read-only little-endian buffers, roots,
  canonical ordering and uniqueness, dense-ID scope, segment roles, resource
  limits, fingerprints, and exact owner lifetime.
- A pre-advertisement amendment retained version 1 while adding an explicit
  sorted anonymous-scope mapping for segmented composites. This prevents
  current-scope identities from leaking across base/member/delta boundaries.
- Direct native registration is exact-schema based. Missing operations,
  descriptor drift, malformed metadata, misalignment, invalid references,
  unsupported tags, or limit violations fail before publication.
- Untrusted buffers are validated and copied before publication. Registered
  retained-native columns and validated immutable mappings are the separately
  tested no-base-copy paths.
- Dense IDs are local to the exact view and may not be persisted in consumer
  caches. PYOCORE remains the stable cross-process representation.

## Implemented checkpoint

- Commit `d3897b8` froze the generated schema, public fallback producer,
  independent reference decoder, hostile-descriptor checks, and version
  decision.
- Commits `ef9ffc2` and `4e95485` connected the public cached view while
  preserving lazy backend import and capability isolation.
- Commit `2b22e9e` generated the native-side schema contract from the same
  ledger used by Python.
- Commit `bf0720a` added exact anonymous-scope remapping for segmented views.
- Commits `054138f`, `5050f69`, and `47244eb` registered direct column
  operations and published retained direct/segmented owners through the public
  view boundary.
- Commit `3df754d` published encoded columns directly from validated mapped
  wire storage; commit `5835136` exposed the exact descriptor digest; commit
  `677084d` made retained PYOCORE encoding consume attested columns rather than
  scalar model rows.
- Core-owned integration fixtures cover Exact-OM, pyELK, pyHermiT, projector,
  and OAEI-style composition boundaries. The pyELK fixture exercises one real
  retained native owner and verifies one encoded-view request, eleven buffers,
  one exporter, and zero scalar facade-row materializations.

## Deliverable ledger

| WP17 deliverable | Status | Evidence / blocker |
|---|---|---|
| Versioned encoded structural schema | implemented candidate | Generated Python/Rust ledgers, exact descriptor digest, runtime/API drift tests, and every-constructor fixtures pass. The version decision withholds advertisement. |
| Complete Python fallback producer | implemented | Scalar canonical roots equal independently decoded column roots for every constructor; hostile inputs and bounded resource failures are covered. |
| Direct retained-native columns | implemented checkpoint | Registered V2 owners publish attested columns through the public view and preserve owner lifetime without scalar facade traversal. Complete installed-wheel format/option coverage remains open with WP16/WP18. |
| Native common indexes | partial | Root/category postings and retained constructor indexes exist. Final consumer-specific compiled indexes remain consumer-owned and are not claimed here. |
| Segmented overlay/composite export | implemented contract checkpoint | Base/member/delta/bridge roles reuse retained owners, publish bounded postings/deltas, and preserve anonymous identity. Large repair-composite memory/performance evidence remains open. |
| Canonical native wire and mmap handoff | implemented checkpoint | Retained encoding consumes columns; verified mmap exposes one immutable exporter and publishes mapped columns without row-wise Python materialization. Installed-artifact and platform lifetime evidence remains open. |
| Independent decoder and coarse consumer harness | implemented | Complete constructor decoding, transport parity, OAEI composition, pyHermiT contract, projector contract, and real pyELK owner fixtures pass locally. External compilers retain ownership of their native implementation and capability claims. |
| Counter invariants | partial local pass | Focused fixtures assert no scalar materialization and expected view/wire/mmap deltas. A complete installed consumer matrix proving zero parser, resolver, encoder/decoder, scalar, and base-copy deltas is still required. |
| Compatibility/version coordination | candidate handoffs implemented | Companion repositories consume or validate the candidate contract behind disabled capabilities. Final clean revision pins and a refreshed cross-repository compatibility manifest remain open. |
| Capability promotion | withheld | Installed-wheel cross-backend, native consumer, corpus, performance, lifetime, sanitizer/fuzz, and release gates have not all passed. |

## Verification evidence

The WP17-focused source-tree matrix was run against the pinned native extension
at the committed implementation ancestry through `677084d`:

```text
PYTHONPATH=src:. \
PYOWL_CORE_TEST_NATIVE_LIBRARY=/private/tmp/pyowlcore-direct-target.TDcfFb/debug/lib_native.dylib \
.venv/bin/python -m pytest -q \
  tests/native/encoded_views tests/native/wire \
  tests/integration/consumers tests/consumer_contract tests/integration/cache

102 passed, 2987 subtests passed in 15.26s
```

The generated-schema drift check passed. Ruff passed for the WP17 Python
surface, and strict mypy passed for the encoded-view modules. These are local
source-tree results. They do not claim an isolated installed wheel, external
corpus, sanitizer, fuzz, platform matrix, approved benchmark host, or released
consumer artifact.

## Acceptance state

WP17 acceptance remains open. The repository-owned schema, fallback producer,
retained direct columns, mapped columns, segmented ownership rules, independent
decoder, and public handoff fixtures are implemented, but promotion requires
all of the following to be recorded from clean coordinated revisions:

- an installed-wheel forced-native matrix spanning direct, mmap, overlay, and
  composite owners, hostile/negative cases, and the WP16 format/option matrix;
- a public native operation in each required consumer path with fallback
  disabled, exact identity/result parity, and complete zero-forbidden-work
  counter evidence;
- lifetime, close/fork/concurrency, corruption, allocation-failure,
  sanitizer/fuzz, alignment/endianness, and supported-platform evidence;
- representative corpus and repair-composite memory/copy/performance evidence;
  and
- a refreshed compatibility manifest with exact final core and consumer
  revisions, supported ranges, artifact identities, and passing release gates.

Until those gates pass, schema 1 remains a frozen candidate only. No package may
infer support from package version or private extension symbols, and the core
must not advertise `pyowl-core/structural-columns` in public capabilities.
